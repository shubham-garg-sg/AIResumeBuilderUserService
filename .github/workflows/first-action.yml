name: AI Resume Builder Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: |
          echo "Hello World"
          echo "Hello India"
      - name: Print goodbye
        run: echo "Goodbye"
  
